const API_BASE_URL_DEVELOPMENT = "https://localhost:7172";

const ENDPOINTS ={
    RIDE_HISTORY: "api/RideHistory",
}

const development = {
    API_URL_RIDE_HISTORY: `${API_BASE_URL_DEVELOPMENT}/${ENDPOINTS.RIDE_HISTORY}`,
}

export default development;